import styled from "styled-components";

export const FlexibleCardBase = styled.article`
  background: var(--card-bg);
  box-shadow: var(--card-shadow);
  color: var(--card-text);
  padding: 1rem;
`;

export default FlexibleCardBase;
